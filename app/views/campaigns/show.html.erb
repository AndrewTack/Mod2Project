<div class="container">
<h1><%=@campaign.title%></h1>
<h1><%=@campaign.brand.name%></h1>
<h2><%=@campaign.category%></h2>
<h3><%=@campaign.type_of%></h3>
<h3><%=@campaign.budget%></h3>
<h3><%=@campaign.summary%></h3>
<h3><%=@campaign.status%></h3>

 <%= image_tag @campaign.image_url, size: "230" %><br>

<%if @campaign.status != "Completed"%>
<%= link_to "Apply to be an influencer", new_campaign_influencer_path(:campaign_id => @campaign.id) %>
<%end%>

 <!-- if logic to make sure noone can delete THE brand besides itself -->
 <!-- take this out-->
  <!-- <%= link_to "delete", @campaign, method: :delete, data: { confirm: "Are You Sure?" } %> -->

</div>

